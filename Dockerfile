# 1. เลือก Base Image เป็น Python (รุ่น Slim เพื่อความเบา)
FROM python:3.10-slim

# 2. 🛠️ ติดตั้ง LibreOffice และฟอนต์ไทย (หัวใจสำคัญ!)
# สั่ง apt-get ทีเดียวจบ ครบทั้งโปรแกรมและฟอนต์
RUN apt-get update && apt-get install -y \
    libreoffice \
    fonts-thai-tlwg \
    fonts-google-sarabun \
    && rm -rf /var/lib/apt/lists/*

# 3. ตั้งค่าโฟลเดอร์ทำงาน
WORKDIR /app

# 4. ก๊อปปี้และติดตั้ง Library Python
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 5. ก๊อปปี้โค้ดทั้งหมดลงไป
COPY . .

# 6. เปิดเซิร์ฟเวอร์ (แก้ชื่อไฟล์ main:app ตามจริงของคุณ)
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
